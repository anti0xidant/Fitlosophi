Run the SQL Script inside the ProdDatabase folder to setup the production database for product demostration. 

Run the SQL inside the TestDatabase folder to setup a separate mock database for the Unit testing project. 